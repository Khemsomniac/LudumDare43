using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Villager : MonoBehaviour
{


    public MouseControlVillager.VillagerGroup belongedGroup; // The group this villager belongs to
    public int villagerGroupIndex; // What's this villager's index in the current group

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
